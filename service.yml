apiVersion: v1
kind: Service
metadata:
  name: ms-payments-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: ms-payments
  ports:
  - protocol: TCP
    port: 30004
    targetPort: 8080
    name: default
